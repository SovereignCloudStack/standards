---
clusters:
  noris-1.34:
    kind: gardener
    config:
      templates:
        shoot: shoot.yaml
        kubeconfig: garden-kubeconfig.yaml
      vars:
        kubernetes_version: "1.34.5"
        num_worker_nodes: 1
        worker_cidr: "10.250.0.0/16"
        machine_type: "SCS-1V-16-50"
        machine_image_name: "flatcar"
        machine_image_version: "4081.3.6"
        region: "nsc-nbg"
        zone: "nbg1"
        floating_pool_name: "external"
        load_balancer_provider: "ovn"
        cloud_profile_name: "openstack"
        credentials_binding_name: "default-openstack-secret"
        garden_namespace: "garden-g5lxqfthcn"
      secrets:
        garden_api_server: "{{ clouds_conf.noris_api_server }}"
        garden_ca_data: "{{ clouds_conf.noris_ca_data }}"
        garden_token: "{{ clouds_conf.noris_token }}"
  syself-1.32:
    kind: clusterstacks
    config:
      kubernetesVersion: '1.32'
      name: 'syself-1-32'  # explicit name because dot in name not permissible
      autoVars: syself  # determine cs_class_name and cs_version automatically using kubernetesVersion
      templates:
        cluster: syself-cluster.yaml
        clusterstack: syself-clusterstack.yaml
        kubeconfig: syself-kubeconfig.yaml
      vars:
        cs_name: hetzner-apalla-1-32
        num_control_nodes: 3
        num_worker_nodes: 3
      secrets:
        token: "{{ clouds_conf.syself_token }}"
  syself-1.33:
    kind: clusterstacks
    config:
      kubernetesVersion: '1.33'
      name: 'syself-1-33'  # explicit name because dot in name not permissible
      autoVars: syself  # determine cs_class_name and cs_version automatically using kubernetesVersion
      templates:
        cluster: syself-cluster.yaml
        clusterstack: syself-clusterstack.yaml
        kubeconfig: syself-kubeconfig.yaml
      vars:
        cs_name: hetzner-apalla-1-33
        num_control_nodes: 3
        num_worker_nodes: 3
      secrets:
        token: "{{ clouds_conf.syself_token }}"
  syself-1.34:
    kind: clusterstacks
    config:
      kubernetesVersion: '1.34'
      name: 'syself-1-34'  # explicit name because dot in name not permissible
      autoVars: syself  # determine cs_class_name and cs_version automatically using kubernetesVersion
      templates:
        cluster: syself-cluster.yaml
        clusterstack: syself-clusterstack.yaml
        kubeconfig: syself-kubeconfig.yaml
      vars:
        cs_name: hetzner-apalla-1-34
        num_control_nodes: 3
        num_worker_nodes: 3
      secrets:
        token: "{{ clouds_conf.syself_token }}"
  # dev clusters using kind
  # latest versions to be found under https://hub.docker.com/r/kindest/node/tags
  kind-1.32:
    kind: kind
    config:
      cluster: kind-cluster.yaml
      image: 'kindest/node:v1.32.3'
  kind-1.31:
    kind: kind
    config:
      cluster: kind-cluster.yaml
      image: 'kindest/node:v1.31.6'
  kind-1.30:
    kind: kind
    config:
      cluster: kind-cluster.yaml
      image: 'kindest/node:v1.30.10'
  kind-1.29:
    kind: kind
    config:
      cluster: kind-cluster.yaml
      image: 'kindest/node:v1.29.14'
